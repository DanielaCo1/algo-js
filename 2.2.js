let min = prompt ("to enter one number");
let max = prompt ("to enter another number");
let current = prompt ("to enter one last number");




if (current >= min && current <= max) {
    console.log("current"); 
} else if  (min > max) {
    console.log("don't understand anything");
    }
